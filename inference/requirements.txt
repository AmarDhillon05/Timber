# API + inference dependencies (separate from the project root requirements.txt)

# core audio / numerics
numpy>=2.0
scipy>=1.10
soundfile>=0.12

# models (AST + CLAP load from the HF Hub; BEATs code is bundled in beats/)
torch>=2.1
torchaudio>=2.1   # BEATs fbank features (must match the installed torch build)
transformers>=4.40

# API
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # required by FastAPI for file uploads

# optional: load inference/.env automatically
python-dotenv>=1.0
